export const mobileMenuStyles = {
    button: 'p-2 text-neutral-500 hover:text-white transition-colors cursor-pointer lg:hidden',
    overlay: 'fixed inset-0 bg-black/80 backdrop-blur-[4px] z-[1040] lg:hidden',
    nav: 'fixed top-20 right-0 w-64 h-[calc(100vh-5rem)] bg-black border-l border-white/10 z-[1050] lg:hidden',
    links: 'flex flex-col p-10 gap-10',
    link: 'text-xs font-black uppercase tracking-[0.15em] text-neutral-500 hover:text-white transition-colors',
};
